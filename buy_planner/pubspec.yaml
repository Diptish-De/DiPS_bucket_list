name: buy_planner
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter
  flutter_native_splash: ^2.4.7
  google_fonts: ^8.0.2
  intl: ^0.20.2
  provider: ^6.1.5+1
  shared_preferences: ^2.5.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - dips-img.png

flutter_native_splash:
  color: "#1E1E1E"
  image: dips-img.png
  color_dark: "#1E1E1E"
  image_dark: dips-img.png
  android_12:
    color: "#1E1E1E"
    color_dark: "#1E1E1E"
